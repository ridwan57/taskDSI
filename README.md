# Question-1 # Vehicle Showroom System

go to folder `qsOne/` then type `npm install` && `npm start` `[npm required]`


# Question-2 # Web Application

go to folder `qsTwo/app` then type `npm install` && `npm start` `[npm required]`
